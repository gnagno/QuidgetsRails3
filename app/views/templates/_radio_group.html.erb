<% choices.each do |choice| -%>
  <%= raw radio(object, choice, {:name => "#{html_options[:name]}", :id => "role_option_#{choice.id}"}) %>
  <%= label_tag("role_option_#{choice.id}", choice.name) %><br />
<% end -%>
